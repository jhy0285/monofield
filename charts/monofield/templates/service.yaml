apiVersion: v1
kind: Service
metadata:
  name: {{ include "monofield.fullname" . }}
  labels:
    {{- include "monofield.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http-proxy
      protocol: TCP
      name: http
  selector:
    {{- include "monofield.selectorLabels" . | nindent 4 }}