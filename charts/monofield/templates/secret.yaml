apiVersion: v1
kind: Secret
metadata:
  name: {{ include "monofield.fullname" . }}
type: Opaque
data:
  {{- if .Values.config.apiToken }}
  MONOFIELD_API_TOKEN: {{ .Values.config.apiToken | b64enc | quote }}
  {{- else }}
  {{- fail "CRITICAL ERROR: config.apiToken cannot be empty." }}
  {{- end }}